services:
  - type: web
    name: screenshot-service
    env: docker
    dockerfilePath: ./screenshot-service/Dockerfile
    dockerContext: ./screenshot-service
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
